# Example OpenShift deployment for the Rust barebones collector.
#
#   oc apply -f deploy/openshift.yaml
#
# The config file lives in a ConfigMap and credentials in a Secret, injected
# as environment variables that the config references via ${VAR}.
apiVersion: v1
kind: ConfigMap
metadata:
  name: collector-rs-config
data:
  config.yaml: |
    default_interval: 5m
    sources:
      - name: idrac-fleet
        type: redfish
        interval: 15m
        config:
          urls: [https://idrac-01.example.com]
          username: svc-collector
          password: ${IDRAC_PASSWORD}
    destinations:
      - name: inventory-db
        type: mongodb
        config:
          url: ${MONGO_URL}
          database: infra
          collection: inventory
---
apiVersion: v1
kind: Secret
metadata:
  name: collector-rs-credentials
type: Opaque
stringData:
  IDRAC_PASSWORD: change-me
  MONGO_URL: mongodb://mongo.example.com:27017
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: collector-rs
  labels:
    app: collector-rs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: collector-rs
  template:
    metadata:
      labels:
        app: collector-rs
    spec:
      containers:
        - name: collector
          image: image-registry.openshift-image-registry.svc:5000/my-project/collector-rs:latest
          args: ["-config", "/etc/collector/config.yaml"]
          envFrom:
            - secretRef:
                name: collector-rs-credentials
          volumeMounts:
            - name: config
              mountPath: /etc/collector
              readOnly: true
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
      volumes:
        - name: config
          configMap:
            name: collector-rs-config
